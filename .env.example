# ==============================================================================
# YetiForge — Environment Configuration
# ==============================================================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#   nano .env
#
# Or run the installer which will configure everything for you:
#   bash install.sh
#
# Fields marked [REQUIRED] must be set. Others have sensible defaults.
#
# ==============================================================================

# --- Telegram Bot [REQUIRED] ---
# Your bot token from @BotFather (https://t.me/BotFather)
# Format: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_BOT_TOKEN=your-bot-token

# Comma-separated list of Telegram user IDs allowed to interact with the bot.
# Find your ID by sending /start to @userinfobot on Telegram.
# Multiple IDs: 123456789,987654321
ALLOWED_USER_IDS=123456789

# --- Claude Code CLI [REQUIRED for AI tasks] ---
# Path to the Claude Code CLI binary.
# Default works if claude is in your PATH. Otherwise use absolute path.
# Install: npm install -g @anthropic-ai/claude-code && claude auth
CLAUDE_CLI_PATH=claude

# Maximum time (milliseconds) to wait for a Claude CLI response.
# 300000 = 5 minutes. Increase for complex tasks.
CLAUDE_TIMEOUT_MS=300000

# --- Project Directory ---
# Default working directory for Claude Code when no project is specified.
# Should be the directory where your code projects live.
DEFAULT_PROJECT_DIR=/home/user/projects/default

# --- Server ---
# Port for the status dashboard API and web UI.
# Default: 3069 — change if this port conflicts with something else.
STATUS_PORT=3069

# Public hostname for webhook callback URLs (optional).
# Set this to your domain name if you have one configured.
# STATUS_HOST=your-domain.com

# Domain used for webhook URLs (optional, usually same as STATUS_HOST).
# WEBHOOK_HOST=your-domain.com

# --- Data Storage ---
# Directory for persistent data: SQLite database, JSON state files, backups.
# Relative to the YetiForge root directory. Use absolute path if needed.
DATA_DIR=./data

# --- Security ---
# Secret key used to sign admin dashboard JWT tokens.
# IMPORTANT: Change this to a random string in production!
# Generate one with: openssl rand -hex 32
ADMIN_JWT_SECRET=yetiforge-admin-secret-change-me-in-prod

# --- Optional: GitHub Integration ---
# GitHub Personal Access Token for repo management features.
# Generate at: https://github.com/settings/tokens
# Needs 'repo' scope for full access.
# GITHUB_PAT=ghp_your_token_here

# --- Optional: VPS Connection (for reference only, not used by the app) ---
# VPS_HOST=your-server-ip
# VPS_USER=ubuntu
# VPS_SSH_KEY=./ssh/your-key.key
